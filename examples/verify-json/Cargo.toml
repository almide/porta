[package]
name = "porta-verify-json"
version = "0.1.0"
edition = "2024"
publish = false

[dependencies]
serde_json = { version = "1", features = ["arbitrary_precision"] }

[profile.release]
opt-level = "s"
lto = true
strip = true
